{
  "mcpServers": {
    "agent-library": {
      "command": "node",
      "args": ["<项目绝对路径>/mcp-server.js"],
      "env": {}
    }
  },
  "_说明": "这是 stdio 模式的参考配置（本机 Agent 用）。部署时把 <项目绝对路径> 替换成你的实际路径，例如：D:/ws/agent_library 或 /opt/agent-library。远端 Agent 用 HTTP 模式：endpoint = http://<服务器>:3000/mcp，不走本文件。"
}
